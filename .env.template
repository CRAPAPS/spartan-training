# ─── SUPABASE ──────────────────────────────────────────────────────────────────
# Production project: https://zlblxrxirhmolkzbfxva.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://zlblxrxirhmolkzbfxva.supabase.co

NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Supabase Dashboard → Project Settings → API → publishable key
# Safe to expose to the browser. RLS policies enforce data isolation.

SUPABASE_SERVICE_ROLE_KEY=
# WARNING: SERVICE ROLE BYPASSES RLS. NEVER expose in client-side code.
# Supabase Dashboard → Project Settings → API → service_role secret

# ─── SCORM CLOUD (Rustici Software) ───────────────────────────────────────────
# Dashboard: https://cloud.scorm.com → Apps → Create App
SCORM_CLOUD_APP_ID=
SCORM_CLOUD_SECRET_KEY=
SCORM_CLOUD_WEBHOOK_SECRET=
# Configure webhook in SCORM Cloud → Notifications → Add Postback URL:
#   https://YOUR_DOMAIN/api/scorm/complete

# ─── NEXT.JS AUTH ─────────────────────────────────────────────────────────────
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:7473
# Production: NEXTAUTH_URL=https://your-domain.com
